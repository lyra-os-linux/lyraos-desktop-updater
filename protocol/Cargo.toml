[package]
name = "lyra-upgrade-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Versioned local protocol for Lyra Upgrade"

[dependencies]
serde.workspace = true
lyra-upgrade-core = { path = "../core" }

[dev-dependencies]
serde_json.workspace = true
